import React from "react";
import "./MenuBar.css"; // Importing CSS for styling

const MenuBar: React.FC = () => {
  return (
    <div className="menu-bar">
      <ul className="menu-list">MAP</ul>
    </div>
  );
};

export default MenuBar;
